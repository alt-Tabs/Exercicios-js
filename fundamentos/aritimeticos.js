const [a, b, c, d]= [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 //muito usado para par ou impar

console.log(soma, subtracao, multiplicacao, -divisao, modulo) // - deixa o resultado negativo(n altera o valor)

